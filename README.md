# check
The script checks if the current user has any processes that have been running for more than one hour in a Linux machine.  If the process has ran for more than one hour, the script will terminate the process, however, it protects the bash shell.
